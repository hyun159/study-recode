# 2026-05-14


# 배운내용
```
was

gunicorn  
uvicorn  
```

# 이해한 내용
```
was?

클라이언츠 ---요청---> 서버  

과거  
백엔드 로직 메모리 생성 -> DB 연결 -> 데이터 처리 -> 메모리 파기, DB연결 해제  
매 요청 마다 메모리 생성 - 종료, db 연결 - 해제를 반복  


현재  
메모리 공간 확보(쓰레드)와 DB연결을 미리 확보 -> 데이터 처리 -> 스레드반납, DB연결 반납  
was는 백엔드 로직이 사용할 메모리와 db를 미리 할당하여 많은 요청을 빠르게 처리할 수 있다.  


gunicorn?  
파이썬의 was  
동기처리 이므로 느리다, 워커 관리가 우수하여 마스터 워커로 사용

uvicorn?
파이썬의 was
비동기처리를 하며 빠르다, 워커로 주로 사용.


gunicorn을 마스터워커로 사용하며 ubicorn을 워커로 사용하는 하이브리드 형태로도 구축가능
systemd에 마스터워커를 등록한다.
```
